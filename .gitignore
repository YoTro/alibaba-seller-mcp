__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
# Local runtime state (OAuth tokens, usage log) — never commit secrets
.alibaba_seller_mcp/
.DS_Store

# Real product folders (photos, briefs with brand data) stay local; only the template is tracked
products/*
!products/example/
